{% extends 'base.html.twig' %}

{% block body %}

    <h1>Liste des thèmes</h1>
    <table class="table">
        <tbody>
        {% for theme in themeList %}
            <tr>
                <td>
                    <a href="{{ path('theme_details', {'id': theme.id}) }}">
                        {{ theme.name }}
                    </a>
                </td>
                <td>{{ theme.numberofPosts | length }} posts</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    {{ form(postForm) }}

{% endblock %}

{% block aside %}
    <h3>Les posts par année</h3>
    <ul>
        {% for item in postList %}
            <li>
                <a href="{{ path('post_by_year', {'year': item.yearPublished}) }}">{{ item.yearPublished }} ({{ item.numberOfPosts }})</a>
            </li>
        {% endfor %}
    </ul>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script src="{{ asset('bundles/ivoryckeditor/ckeditor.js') }}"></script>
{% endblock %}